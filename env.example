# Example environment variables for deployment (rename to .env and set real values)
PORT=8000

# Optional admin credentials for server-side admin backdoor (use only in secure environments)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASS=supersecretpassword

# For external services (placeholders)
# DATABASE_URL=
# SENTRY_DSN=
# OTHER_SECRET=